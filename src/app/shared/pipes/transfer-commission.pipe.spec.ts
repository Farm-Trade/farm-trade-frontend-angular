import { TransferCommissionPipe } from './transfer-commission.pipe';

describe('TransferCommissionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransferCommissionPipe();
    expect(pipe).toBeTruthy();
  });
});
